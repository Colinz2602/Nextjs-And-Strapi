'use client'
import React from 'react'

const AddToCard = () => {
    return (
        <div>
            <button className='btn btn-primary' onClick={() => alert("Clicked")}>Click</button>
        </div>
    )
}

export default AddToCard
